{
  "name": "novavision-web-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/generate-env.js",
    "predev": "node scripts/generate-env.js",
    "dev": "vite --host 0.0.0.0",
    "dev:api": "npx vercel dev",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "lint": "node scripts/check-env.js",
    "stripe:prices": "node scripts/create-stripe-prices.js",
    "deploy": "npx vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "stripe": "^16.12.0"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  }
}
